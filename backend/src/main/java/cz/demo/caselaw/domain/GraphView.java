package cz.demo.caselaw.domain;

import java.util.List;

/** Mermaid diagram generated from the compiled graph + current node states. */
public record GraphView(String mermaid, List<GraphNode> nodes) {}
